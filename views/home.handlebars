<section class="container">
    <h1>{{{ title }}}</h1>
        <section>
            {{#each posts}}
        <section class="card flex-row justify-center">
            <a href="/message/{{id}}">
            <h2>{{title}}</h2>
            </a>
        </section>
            {{/each}}
        </section>


</section>






